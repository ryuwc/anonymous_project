1. 01_nav_footer
   ```
   네비게이션 바를 만드는데 이미지나 텍스트를 넣는 것은 플렉스를 사용하여 넣으면 되기 때문에 어렵지는 않았다.
   다만, 일정한 부분에서는 햄버거 버튼을 넣어줘서 밑에 항목이 뜨게 해야하는데 햄버거 버튼을 넣는데 시간이 오래 걸렸다.
   그 외에는 별다른 어려움은 없었다.
   ```

2. 02_home
   ```
   header 부분을 만드는데는 캐러셀을 사용하여 쉽게 넣었다. 또한, 밑에 카드 를 넣는 것도 부트스트랩의 카드 클래스를 통해 쉽게 만들었다.
   ```

3. 03_community
   ```
   이 과정을 하는데 시간이 제일 많이 걸렸고 가장 어려웠다.
   breakpoint를 이용해 일정 구간에서 나타날 부분, 나타나지 않을 부분을 따로 만들어 작성하였는데, 효율적이진 않은 것 같다. row col 클래스를 사용하면 쉽게 만들어 질 것 같긴 한데, 나에게는 익숙하지 않아 이 방법은 쓰지 않고 만들었다. 다음에 홈페이지를 만든다면 row col 클래스를 좀 더 숙지하고 만들면 더 효율적으로 작업할 수 있을 것 같다.