# pjt02

1. problem_a
   ```pyton
   URL을 통해 원하는 API정보를 불러오는 법을 배웠다.
   지난 json파일을 불러오는 것과 비슷하지만, 그 API안에서 내가 원하는
   정보를 불러오는 것이 아직 서툴다.
   이 부분을 계속 해서 배우고 숙지해야겠다.
   ```

2. problem_b
   ```
   영화 평점이 8점 이상인 목록을 반환하려면
   먼저 새로운 리스트를 만들어 주고, for문을 통해 각 영화의 데이터를 순회해주고
   if문을 통해 key가 'vote_average'인 value가 8 >=이면 새로운 리스트에 추가하면 된다.
   ```

3. problem_c
   ```
   다수의 딕셔너리를 가지고 있는 리스트를 정렬을 해야한다.
   정렬은 key가 'vote_average'를 중심으로 역순으로 정렬해야 한다.
   sorted와 lambda함수를 사용한다.(sorted(lst, key=lambda x:x['vote_average']))
   이러면 평점이 높은 것을 먼저로 정렬이 된다.
   여기서 슬라이싱을 통해 앞의 5개만 빼오면 된다.
   ```

4. problem_d
   ```
   전의 문제들의 개념을 이해하면 조금 응용해서 활용하여 문제를 풀 수 있었다. 
   하지만, search/movie에서 가져오는 값이 영화이름이 아닌 '검색할 수 없는 영화'일 경우 None을 반환하는 과정이 조금 시간이 걸렸다. 간단한 조건이지만, 관점을 조금 다르게 봤었다.
   내가 입력한 조건문은 '검색할 수 없는 영화'의 API를 가져온 변수가 비었을때 None을 반환하는 것이었으나, 사실은 그 아래인 'results'가 비었어야 작동이 되었다.
   ```

5. problem_e
   ```
   problem_d와 비슷하게 접근하면 문제를 풀 수 있다. 다만, 'known_for_department'가 Directing인 사람의 이름을 추가할 때 중복된 이름을 넣으면 안된다. 처음 for문에 조건문을 추가하여 크루리스트에 이름이 있으면 추가안하고, 없으면 추가 이런식으로 해주면 된다. 하지만 뭔가 작동이 잘 안하여 세트로 중복값을 제거했다.
   또한, d에서 None을 반환한 코드와는 다르게 try except 를 사용하여 None을 반환하였다.
   ```


